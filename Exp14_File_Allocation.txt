==================================================================
  Exp14_File_Allocation
==================================================================

################  C PROGRAMS  ################

----- 1_sequential_allocation.c -----
#include <stdio.h>
int main()
{
 int start, length, i;
 printf("Enter Starting Block: ");
 scanf("%d", &start);
 printf("Enter File Length (Number of Blocks): ");
 scanf("%d", &length);
 printf("\nAllocated Blocks:\n");
 for(i = 0; i < length; i++)
 {
 printf("%d ", start + i);
 }
 printf("\n");
 return 0;
}

----- 2_indexed_allocation.c -----
#include <stdio.h>
int main()
{
 int n, indexBlock, blocks[20], i;
 printf("Enter Index Block: ");
 scanf("%d", &indexBlock);
 printf("Enter Number of Blocks: ");
 scanf("%d", &n);
 printf("Enter Block Numbers:\n");
 for(i = 0; i < n; i++)
 {
 scanf("%d", &blocks[i]);
 }
 printf("\nIndex Block : %d\n", indexBlock);
 printf("Allocated Blocks : ");
 for(i = 0; i < n; i++)
 {
 printf("%d ", blocks[i]);
 }
 printf("\n");
 return 0;
}

----- 3_linked_allocation.c -----
#include <stdio.h>
int main()
{
 int n, blocks[20], i;
 printf("Enter Number of Blocks: ");
 scanf("%d", &n);
 printf("Enter Block Numbers:\n");
 for(i = 0; i < n; i++)
 {
 scanf("%d", &blocks[i]);
 }
 printf("\nLinked Allocation:\n");
 for(i = 0; i < n - 1; i++)
 {
 printf("%d --> ", blocks[i]);
 }
 printf("%d --> NULL\n", blocks[n - 1]);
 return 0;
}

################  SHELL PROGRAMS  ################

----- 1_sequential_allocation.sh -----
#!/bin/bash
echo "Enter Starting Block:"
read start
echo "Enter File Length:"
read length
echo "Allocated Blocks:"
for ((i=0;i<length;i++))
do
 echo -n "$((start+i)) "
done
echo

----- 2_indexed_allocation.sh -----
#!/bin/bash
echo "Enter Index Block:"
read index
echo "Enter Number of Blocks:"
read n
echo "Enter Block Numbers:"
for ((i=0;i<n;i++))
do
 read block[$i]
done
echo "Index Block : $index"
echo -n "Allocated Blocks : "
for ((i=0;i<n;i++))
do
 echo -n "${block[$i]} "
done
echo

----- 3_linked_allocation.sh -----
#!/bin/bash
echo "Enter Number of Blocks:"
read n
echo "Enter Block Numbers:"
for ((i=0;i<n;i++))
do
 read block[$i]
done
echo "Linked Allocation:"
for ((i=0;i<n-1;i++))
do
 echo -n "${block[$i]} --> "
done
echo "${block[$((n-1))]} --> NULL"

################  OUTPUT  ################

=== 1. Sequential Allocation (C) - Start:10, Length:5 ===
Enter Starting Block: Enter File Length (Number of Blocks): 
Allocated Blocks:
10 11 12 13 14 

=== 2. Indexed Allocation (C) - IndexBlock:20, Blocks: 3 5 7 9 ===
Enter Index Block: Enter Number of Blocks: Enter Block Numbers:

Index Block : 20
Allocated Blocks : 3 5 7 9 

=== 3. Linked Allocation (C) - Blocks: 4 8 15 16 23 ===
Enter Number of Blocks: Enter Block Numbers:

Linked Allocation:
4 --> 8 --> 15 --> 16 --> 23 --> NULL

=== 1. Sequential Allocation (Shell) ===
Enter Starting Block:
Enter File Length:
Allocated Blocks:
10 11 12 13 14 

=== 2. Indexed Allocation (Shell) ===
Enter Index Block:
Enter Number of Blocks:
Enter Block Numbers:
Index Block : 20
Allocated Blocks : 3 5 7 9 

=== 3. Linked Allocation (Shell) ===
Enter Number of Blocks:
Enter Block Numbers:
Linked Allocation:
4 --> 8 --> 15 --> 16 --> 23 --> NULL
